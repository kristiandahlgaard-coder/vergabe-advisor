# Vergabe-Advisor Backend — Environment Configuration
# Diese Werte müssen auf Railway in den "Variables" gesetzt werden

# PostgreSQL Connection
DATABASE_URL=postgresql://vergabe_user:vergabe_pass@localhost:5432/vergabe_db

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development

# JWT Secret (ÄNDERE DIES IN PRODUCTION!)
JWT_SECRET=dev_secret_change_in_production_immediately

# Email (später)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-password
# SMTP_FROM=noreply@vergabe-advisor.de
